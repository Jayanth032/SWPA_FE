// 16
function sum(a, b) {
  return a + b;
}

// 17
function describePerson(name, color) {
  return `${name}'s favorite color is ${color}.`;
}

// 18
function averageOfThree(a, b, c) {
  return (a + b + c) / 3;
}

// 19
function repeatString(str, times) {
  return str.repeat(times);
}

// 20
function getArrayLength(arr) {
  return arr.length;
}
