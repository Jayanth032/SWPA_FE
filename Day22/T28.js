let isUser = true;
let isAdmin = false;

console.log("User AND Admin:", isUser && isAdmin); // false, both must be true
console.log("User OR Admin:", isUser || isAdmin);  // true, at least one is true
console.log("NOT Admin:", !isAdmin);               // true, negation of false
